package runners;

import io.cucumber.testng.AbstractTestNGCucumberTests;
import io.cucumber.testng.CucumberOptions;
import org.testng.annotations.Test;

@CucumberOptions(
    features = "src/test/resources/features/invalid.feature", // Path to the feature file
    glue = "stepDefinitions", // ✅ Use package name, not class name
    plugin = {"pretty", "html:target/cucumber-reports/InvalidLogin.html"},
    monochrome = true,
    tags = "@InvalidLogin"
)
@Test
public class InvalidLoginRunner extends AbstractTestNGCucumberTests {
}
